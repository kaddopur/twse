{
    "private": true,
    "scripts": {
        "build": "lerna run build",
        "clean": "rimraf packages/**/dist"
    },
    "devDependencies": {
        "lerna": "^2.8.0"
    },
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "rimraf": "^2.6.2"
    }
}
